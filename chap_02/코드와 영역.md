
```
#include <stdio.h>

int gval; // 전역변수 데이터

int main() {
	int localval;
	char *name;
        name= malloc(20);  // 힙 영역
	int sum = 5;  // 지역 변수는 스택
	localval = 10;
	localval = add( 10, 20);
	printf("%d\n", sum);
}
int add(int a, int b){
	int sum ;
  	sum = a+b;
	return sum;
}
```
